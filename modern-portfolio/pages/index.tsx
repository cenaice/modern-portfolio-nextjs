import React from "react";
import Head from "next/head"; // Import Next.js Head for setting page metadata
import RootLayout from "../components/RootLayout"; // Import your RootLayout component or adjust the path as needed

const Home: React.FC = () => {
  return (
    <RootLayout>
    </RootLayout>
  );
};

export default Home;